description = 'Delivery4j Broker PostgreSQL'

dependencies {
    api 'org.postgresql:postgresql:42.7.11'
}

javadoc {
    options {
        links 'https://javadoc.io/doc/org.postgresql/postgresql/latest/'
    }
}